## Summary

<!-- What does this PR do? 1-3 sentences. -->

## Changes

<!-- Bullet list of what changed. -->

-

## Test Plan

<!-- How was this tested? -->

- [ ] Tests pass (`pytest` for CLI, `pnpm test` for MCP server)
- [ ] No secrets or credentials in committed files
- [ ] Docs updated if behavior changed
- [ ] Linting passes (`ruff check` for Python, `tsc --noEmit` for TypeScript)
